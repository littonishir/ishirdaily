<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.litton.ishirdaily">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ReflectKActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".recyclerview.Main2Activity" />
        <activity android:name=".recyclerview.Main3Activity" />
        <activity android:name=".eventbus.LoginActivity" />
        <activity android:name=".eventbus.Main5Activity" />
        <activity android:name=".eventbus.Main6Activity" />
        <activity android:name=".eventbus.BaseEventBusActivity" />
        <!--<activity android:name=".ReflectKActivity"></activity>-->
    </application>

</manifest>